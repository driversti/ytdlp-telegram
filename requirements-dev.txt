# Development and testing dependencies
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
respx>=0.20.0

# Code quality
# Pinned: an unbounded range makes CI fail on someone else's schedule. ruff
# 0.16.0 enabled rules that flag 91 pre-existing issues across this repo, which
# turned main red without a single line of code changing. Bump deliberately,
# with the cleanup in the same commit.
ruff==0.15.12
